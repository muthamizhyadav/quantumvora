{
  "name": "quantumvora",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "build": "npx tailwindcss -i src/input.css -o dist/styles.css --minify && cp index.html style.css script.js dist/ && cp -r public dist/",
    "watch": "npx tailwindcss -i src/input.css -o dist/styles.css --watch",
    "deploy": "wrangler pages deploy .",
    "preview": "wrangler pages dev ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "tailwindcss": "^4.3.2"
  }
}